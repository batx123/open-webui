apiVersion: v1
kind: Service
metadata:
  name: ollama-service
  namespace: open-webui
spec:
  selector:
    app: ollama
  type: LoadBalancer  # Use LoadBalancer if you're on a cloud that supports it
  ports:
  - protocol: TCP
    port: 11434
    targetPort: 11434
